document.addEventListener("DOMContentLoaded",()=>{$("#loginForm")?.addEventListener("submit",e=>{e.preventDefault();let u=get("dc_users").find(x=>x.email===$("#email").value.toLowerCase()&&x.password===$("#password").value);if(!u)return toast("Incorrect login details.");set("dc_current",u);location.href=u.role==="patient"?"patient/dashboard.html":u.role==="doctor"?"doctor/dashboard.html":"admin/dashboard.html"});$("#registerForm")?.addEventListener("submit",e=>{e.preventDefault();if($("#pw").value!==$("#pw2").value)return toast("Passwords do not match.");let u={id:"p"+Date.now(),name:$("#name").value,email:$("#mail").value.toLowerCase(),phone:$("#phone").value,dob:$("#dob").value,role:"patient",password:$("#pw").value},a=get("dc_users");if(a.some(x=>x.email===u.email))return toast("Email already registered.");a.push(u);set("dc_users",a);set("dc_current",u);location.href="patient/dashboard.html"})})