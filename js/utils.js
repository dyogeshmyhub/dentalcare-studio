function cancelAppointment(id){let appointments=get("dc_appointments"),appointment=appointments.find(x=>x.id===id);if(!appointment||appointment.status==="Cancelled")return;if(!confirm("Cancel this appointment?"))return;appointment.status="Cancelled";set("dc_appointments",appointments);toast("Appointment cancelled.");setTimeout(()=>location.reload(),350)}
const $=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)];function get(k,d=[]){try{return JSON.parse(localStorage.getItem(k))??d}catch{return d}}function set(k,v){localStorage.setItem(k,JSON.stringify(v))}function user(){return get("dc_current",null)}function money(n){return"₹"+Number(n).toLocaleString("en-IN")}function dateFmt(d){return new Date(d+"T00:00:00").toLocaleDateString("en-IN",{day:"2-digit",month:"short",year:"numeric"})}function esc(v=""){return String(v).replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[m]))}function toast(m){let x=$("#toast");if(!x)return alert(m);x.textContent=m;x.className="toast show";setTimeout(()=>x.className="toast",2800)}function logout(){localStorage.removeItem("dc_current");location.href="../index.html"}function status(s){return`<span class="status ${s.toLowerCase().replaceAll(" ","-")}">${esc(s)}</span>`}function tokenModal(a){let x=document.createElement("div");x.className="modal";x.innerHTML=`<div class="token-card"><span class=eyebrow>Appointment confirmed</span><h2>Your queue token</h2><div class=token>${a.token}</div><b>${esc(a.doctorName)}</b><p>${dateFmt(a.date)} · ${a.time}</p><div class=mini><div><b>4</b><small>patients ahead</small></div><div><b>32m</b><small>estimated wait</small></div></div><button class="btn primary full" onclick="this.closest('.modal').remove()">Done</button></div>`;document.body.appendChild(x)}
function downloadSlip(a){let html=`<!doctype html><html><head><meta charset="UTF-8"><title>DentalCare Appointment ${esc(a.id)}</title><style>body{font-family:Arial,sans-serif;color:#183a38;padding:40px;max-width:620px;margin:auto}h1{margin-bottom:4px}header{border-bottom:3px solid #b7d64a;padding-bottom:20px;margin-bottom:24px}.token{font-size:42px;font-weight:bold;color:#19736d;margin:18px 0}.row{display:flex;justify-content:space-between;border-bottom:1px solid #ddd;padding:12px 0}.muted{color:#687875;font-size:12px}</style></head><body><header><h1>DentalCare Studio</h1><div class="muted">Appointment confirmation slip</div></header><div class="token">${esc(a.token)}</div><div class="row"><b>Appointment ID</b><span>${esc(a.id)}</span></div><div class="row"><b>Patient</b><span>${esc(a.patientName)}</span></div><div class="row"><b>Dentist</b><span>${esc(a.doctorName)}</span></div><div class="row"><b>Service</b><span>${esc(a.service)}</span></div><div class="row"><b>Date</b><span>${dateFmt(a.date)}</span></div><div class="row"><b>Time</b><span>${esc(a.time)}</span></div><p class="muted">Please arrive 10 minutes before your appointment.</p></body></html>`;let url=URL.createObjectURL(new Blob([html],{type:"text/html"})),link=document.createElement("a");link.href=url;link.download=`dentalcare-${a.id}-appointment.html`;link.click();setTimeout(()=>URL.revokeObjectURL(url),1000)}
function viewAppointment(id){let a=get("dc_appointments").find(x=>x.id===id);if(!a)return;let x=document.createElement("div");x.className="modal";x.innerHTML=`<div class="token-card appointment-detail"><span class=eyebrow>Appointment details</span><h2>${esc(a.doctorName)}</h2><p>${esc(a.service)}</p><div class=mini><div><b>${dateFmt(a.date)}</b><small>Date</small></div><div><b>${esc(a.time)}</b><small>Time</small></div></div><p><b>Queue token ${esc(a.token)}</b><br>${status(a.status)}</p><div class="modal-actions"><button class="btn primary" onclick="downloadSlip(get('dc_appointments').find(x=>x.id==='${a.id}'))">Download slip</button><button class="btn ghost" onclick="this.closest('.modal').remove()">Close</button></div></div>`;document.body.appendChild(x)}