function render(){let q=($("#q")?.value||"").toLowerCase(),s=$("#service")?.value||new URLSearchParams(location.search).get("service")||"",sort=$("#sort")?.value||"rating",a=doctors.filter(d=>(!q||`${d.name} ${d.specialty}`.toLowerCase().includes(q))&&(!s||d.service===s));a.sort((x,y)=>sort==="experience"?y.experience-x.experience:sort==="fee"?x.fee-y.fee:y.rating-x.rating);$("#doctorList").innerHTML=a.map(doctorCard).join("")||"<div class=panel>No dentists found.</div>"}document.addEventListener("DOMContentLoaded",()=>{if(!$("#doctorList"))return;$("#service").innerHTML+=[...services].map(s=>`<option value=${s.id}>${s.name}</option>`).join("");["q","service","sort"].forEach(x=>$("#"+x).addEventListener("input",render));render()})